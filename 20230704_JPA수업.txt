0704 수업메모 - JPA, React

도메인 -> 여러개의 Entity를 가짐

BaseEntity -> 추상클래스

@MappedSuperclass -> 테이블로 만들어지지 않게 설정해주는 어노테이션

JPQL(Java Persistence Query Language)
JPQL은 엔티티 객체를 조회하는 객체지향 쿼리다.

QueryDSL
코끼리 클릭 > build > clean 실행
other > compileJava 실행
파일클릭 > build > generated > annotationProcessor에 java파일이 있어야함

biild > generated > Qjava